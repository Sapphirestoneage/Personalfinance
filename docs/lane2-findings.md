# Lane 2 findings

Generated by the lane 2 suites from `tests/reports/*.json`; regenerated in full on every run. Do not edit by hand. If a fixture is wrong, change its spec in `tests/tools/build-households.js`; if an engine is wrong, write the fix as a proposal in `docs/lane2-proposals.md`. A fixture is never changed to match an engine, and no engine is changed from this lane.

## Section 1: the synthetic household corpus

Corpus: 24 archetypes, 6 edge cases. Sweep: 231 household-first functions across 64 engines, 6930 calls. Source: `tests/corpus.test.js`.

### Where the engine disagrees with the hand arithmetic

None. Every `known` value in every fixture is within 1% of the engine, and every expected incompleteness is incomplete.

### Throws, NaN and forbidden negatives

None. No household-first engine function threw when called with real arguments, nothing returned NaN or Infinity, and no runway, tax, months or FI figure came back negative.

### Functions the sweep could not call generically

These take an argument the sweep has no real value for (a skill, a goal, a template, an offer). They were called with that argument undefined; a throw there is the missing argument, not a finding. The property files under `tests/properties/` give each a real value where one exists.

| function | parameters | needs |
|---|---|---|
| careermove.offerHousehold | household, offer | offer |
| careermove.offerWork | household, offer | offer |
| skills.due | household, skill, today | skill |
| skills.isActive | household, skill | skill |
| skills.returnOnEffort | household, skill, tables, ctx | skill, ctx |
| skills.state | household, skill | skill |
| skills.synergyMultiplier | household, skill, tables | skill |
| skills.unmetPrereqs | household, skill | skill |
| skills.valuePerYear | household, skill, tables, ctx | skill, ctx |
| skills.verifies | household, skill | skill |
| studentloans.loansOnly | household, loans | loans |
| week.costOf | household, tables, block | block |

### Notes the corpus itself raises

- The effective-rate table is flat by gross and filing status. The `state` field, a 1099 income type, and the SE tax do not change the estimate, so `self-employed-lumpy`, `house-hacker`, `variable-hustle` and `geo-arb` carry the same rate a W-2 earner in any state would. The fixtures agree with the engine because they use the same table; the note is that the table is approximate (its own `precision` says so), not that the engine is wrong. Section 3 (tax_brackets.json, states.json) is where a sharper figure would come from.
- `retired-early` and `between-jobs` have no income source, so take-home and the savings rate are incomplete. Portfolio draws and unemployment benefits are not income sources in the current shape; the engines that read them (`decumulation`, `betweenjobs`) are swept but not compared to a known value here.
- `zero-income` types a gross of $0: the engine estimates $0 of tax and $0 of take-home and refuses a savings rate (zero denominator). Both match the fixture.
- `forty-debts` sits exactly on the $75,000 single boundary. The table rule is `gross <= upToGrossIncome`, so it lands in the 0.19 band; the fixture says so in `band`.

## Section 2: property tests

Seed 20260910, 100 cases per property, 66 files for 64 engines, 282 properties, 6 failing. Source: `tests/properties/run.js`. A failing row carries the shrunk spec (dollars) that breaks it; `tests/properties/_harness.js` `build()` turns it back into a household.

### Failing properties

**adventure: applying a shock leaves the household byte-identical, and a headwind never ends with a bigger pot**

- Reason: jobloss ended with -10074 net > -11080 unshocked (portfolio -5037 vs -5540, borrowed 5037 vs 5540)
- Minimal spec: `[{"year":1955,"gross":1,"match":{"pct":0.5,"cap":0.03},"filingStatus":"single","cash":0,"invested":0,"retirementValue":0,"property":0,"vehicle":null,"debts":[],"food":1,"accommodation":0,"transportation":0,"wants":0},"drift","jobloss"]`
- Note: Net of anything borrowed: a shock that stops income can leave a smaller pot and a bigger loan, and the two are compared together.

**blocks: the same blocks applied twice give the same household**

- Reason: differs only in generated ids (Schema.newId) for a source or asset the block created; every number matches
- Minimal spec: `[{"people":[{"year":1950,"status":"employed","income":[]}],"filingStatus":"single","state":"CA","assets":[],"debts":[],"fat":{"food":0,"accommodation":0,"transportation":0,"wants":null},"hasDebt":null,"contributionPercent":0},{"id":"l","label":"line","source":"test","confidence":"convention","path":"income.grossAnnualCents","delta":0,"kind":"monthly"}]`

**projection: a level payment times its months repays at least the principal**

- Reason: negative interest -18
- Minimal spec: `{"principal":100,"rate":0,"months":41}`
- Note: Rounding to the cent each month may leave the total up to one cent per month short of the principal.

**ratios: cents in, cents out: no *Cents field carries a fraction**

- Reason: all.rows.0.result.monthlyGrossIncomeCents = 2513008.3333333335
- Minimal spec: `{"people":[{"year":1950,"status":"employed","income":[{"gross":301561,"type":"w2","match":{"pct":0.5,"cap":0.03}}]}],"filingStatus":"single","state":null,"assets":[],"debts":[],"fat":{"food":0,"accommodation":0,"transportation":0,"wants":0},"hasDebt":false,"contributionPercent":0}`

**skills: cents in, cents out: no *Cents field carries a fraction**

- Reason: available.13.returnOnEffortCents = 2911.0512129380054, available.14.returnOnEffortCents = 1951.219512195122, available.15.returnOnEffortCents = 1940.7008086253368
- Minimal spec: `{"people":[{"year":1950,"status":"employed","income":[]}],"filingStatus":"single","state":"CA","assets":[],"debts":[],"fat":{"food":0,"accommodation":0,"transportation":null,"wants":0},"hasDebt":null,"contributionPercent":null}`

**tier0: cents in, cents out: no *Cents field carries a fraction**

- Reason: debtToIncome.monthlyGrossIncomeCents = 2513008.3333333335
- Minimal spec: `{"people":[{"year":1950,"status":"employed","income":[{"gross":301561,"type":"w2","match":{"pct":0.5,"cap":0.03}}]}],"filingStatus":"single","state":null,"assets":[],"debts":[],"fat":{"food":0,"accommodation":0,"transportation":0,"wants":0},"hasDebt":false,"contributionPercent":0}`

### Notes from the property files: what could not be held yet, and what a failure means

- adventure: Six months without work (jobloss) on a household already in deficit ends with LESS borrowed than the unshocked run: in the shrunk case (gross $1 a year, $1 a month of food) the shock year adds $605 of borrowing against a $1,159 shortfall, where every other year adds the full $1,108. The half-year of lost income seems to be applied to the net of the year rather than to the income alone. Small numbers, but the sign is wrong: a headwind helped.
- decumulation: Sequence of returns (a bad-early ending pot at most a good-early one at equal average return) has no engine to hold it to: drawPath, vpw.plan, projection.pathCents and windfall.run all take one constant rate. Deferred until an engine consumes a return sequence.
- levers: There is no remove(): a lever returns a new household and the original is untouched, so "apply then remove is byte-identical" is held as "the input is byte-identical after apply" plus "scale 0 changes nothing".
- ratios: context(), all() and scored() read Date.now() when opts.now is absent and echo it as `now`; two calls a millisecond apart differ. The suite passes a fixed `now`, so the clock read is treated as an input rather than hidden state. Rooms that call without `now` get a wall-clock stamp in the result.
- statement: After-tax value of a holding (at most its listed value; Roth equal to listed) has no engine yet: after-tax assets arrive with section 15 of the master build. The property is deferred until the shape lands.
- windfall: run() returns `lump` and `spread` as fractional cents while the path rows are rounded; only the rows carry a Cents suffix, so the generic whole-cents check does not see the two headline figures. Recorded here rather than as a failure because the field names do not claim to be cents.

### What each engine is held to

| engine | properties |
|---|---|
| accounts | compareAccounts on random inputs: no throw, no NaN, whole cents, the same answer twice |
| adventure | no throw, no NaN or Infinity, on any valid household (5 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction; FAIL: applying a shock leaves the household byte-identical, and a headwind never ends with a bigger pot; a tailwind never ends with a smaller pot; every way through but Drift holds or cuts year-one spending against the baseline |
| advice | no throw, no NaN or Infinity, on any valid household (3 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| benchmarks | no throw, no NaN or Infinity, on any valid household (9 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| betweenjobs | no throw, no NaN or Infinity, on any valid household (3 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| blocks | no blocks: the household comes back with every number unchanged and the input untouched; two additive blocks equal the sum of each applied alone (spending, cash, gross); FAIL: the same blocks applied twice give the same household |
| buckets | no throw, no NaN or Infinity, on any valid household (4 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| budget | no throw, no NaN or Infinity, on any valid household (3 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| calendar | no throw, no NaN or Infinity, on any valid household (2 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| careermove | no throw, no NaN or Infinity, on any valid household (2 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| cashflow | no throw, no NaN or Infinity, on any valid household (10 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| credential | credentialROI on random inputs: no throw, no NaN, whole cents, the same answer twice |
| debt | no throw, no NaN or Infinity, on any valid household (5 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction; a payoff pays back at least every balance, and interest is never negative; avalanche never costs more interest than snowball; more extra a month never takes more months or costs more interest; the cheapest strategy in a comparison really is the cheapest |
| decumulation | no throw, no NaN or Infinity, on any valid household (2 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction; a draw path never ends higher with a lower return, and a bigger draw never ends higher |
| draftt | no throw, no NaN or Infinity, on any valid household (3 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| dreamline | no throw, no NaN or Infinity, on any valid household (7 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| enough | no throw, no NaN or Infinity, on any valid household (6 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| estate | no throw, no NaN or Infinity, on any valid household (2 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| events | no throw, no NaN or Infinity, on any valid household (2 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| exercises | no throw, no NaN or Infinity, on any valid household (1 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| fire | no throw, no NaN or Infinity, on any valid household (3 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction; every variant target rises when spending rises; lean <= standard <= chubby <= fat; the FI date never moves earlier when spending rises or income falls |
| foo | no throw, no NaN or Infinity, on any valid household (5 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| fulfillment | no throw, no NaN or Infinity, on any valid household (2 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| giving | no throw, no NaN or Infinity, on any valid household (2 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| goals | no throw, no NaN or Infinity, on any valid household (1 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| hassle | no throw, no NaN or Infinity, on any valid household (2 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction; returnOnHassle on random inputs: no throw, no NaN, whole cents, the same answer twice |
| health | no throw, no NaN or Infinity, on any valid household (2 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| history | no throw, no NaN or Infinity, on any valid household (1 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| hourly | no throw, no NaN or Infinity, on any valid household (1 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| housing | no throw, no NaN or Infinity, on any valid household (1 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction; a mortgage payment repays the principal and rises with the rate |
| income | no throw, no NaN or Infinity, on any valid household (1 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction; annualising a stored annual figure returns it unchanged; a monthly rate times twelve |
| kids | no throw, no NaN or Infinity, on any valid household (1 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| layouts |  |
| ledger | no throw, no NaN or Infinity, on any valid household (4 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| levers | applying a lever never touches the household it was given; a lever at scale 0 changes no number: only the record of its application; the same lever applied twice to the same household gives the same household |
| partner | no throw, no NaN or Infinity, on any valid household (1 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| presets | no throw, no NaN or Infinity, on any valid household (6 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| projection | future value never falls when the rate or the contribution rises; FAIL: a level payment times its months repays at least the principal; more contribution never takes longer to reach a target; the same input gives the same output twice; cents in, cents out on every projection |
| protection | no throw, no NaN or Infinity, on any valid household (1 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| purchase | no throw, no NaN or Infinity, on any valid household (2 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| quickmath | no throw, no NaN or Infinity, on any valid household (2 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction; switching to a higher-yield account never loses money, and the gain grows with the spread; cost per use falls, or holds, with more uses |
| ratios | no throw, no NaN or Infinity, on any valid household (4 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; FAIL: cents in, cents out: no *Cents field carries a fraction |
| rerank | no throw, no NaN or Infinity, on any valid household (3 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| reversibility |  |
| runway | no throw, no NaN or Infinity, on any valid household (2 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction; runway never counts property or vehicles; runway falls, or holds, when cash falls; runway is a whole number of months between 0 and the horizon; cutting spending never shortens the runway |
| selfemployed | no throw, no NaN or Infinity, on any valid household (2 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction; self-employment tax is at most 15.3% of net profit plus the additional Medicare tax, never negative, never falls as profit rises |
| sidehustle | no throw, no NaN or Infinity, on any valid household (1 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction; sideHustle on random inputs: no throw, no NaN, whole cents, the same answer twice |
| skills | no throw, no NaN or Infinity, on any valid household (11 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; FAIL: cents in, cents out: no *Cents field carries a fraction |
| skilltree | no throw, no NaN or Infinity, on any valid household (3 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| ss | no throw, no NaN or Infinity, on any valid household (1 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction; the primary insurance amount never falls as average indexed earnings rise, and is never above them; the claiming factor never falls as the claiming age rises |
| statement | no throw, no NaN or Infinity, on any valid household (7 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction; confidence-weighted net worth never exceeds plain net worth (weights are at most 1); the portfolios add up to every valued asset, each in exactly one bucket |
| statements | no throw, no NaN or Infinity, on any valid household (5 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| studentloans | no throw, no NaN or Infinity, on any valid household (2 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| swan | no throw, no NaN or Infinity, on any valid household (4 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| tax | no throw, no NaN or Infinity, on any valid household (5 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction; ordinary tax never exceeds the income it is charged on and never falls as income rises; FICA is at most 7.65% of wages plus the additional Medicare tax, and never negative; the whole estimate never exceeds gross and never falls as gross rises; capital gains tax is zero on no gain and never negative; the same input gives the same estimate twice |
| taxroom | no throw, no NaN or Infinity, on any valid household (2 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| tier0 | no throw, no NaN or Infinity, on any valid household (12 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; FAIL: cents in, cents out: no *Cents field carries a fraction; take-home never exceeds gross, and the estimated tax is never negative; annual savings never exceed take-home (excluding and including match); FI target rises when spending rises and falls when it falls, everything else fixed; years to FI never move earlier when spending rises or income falls; emergency-fund months fall when cash falls and ignore property and vehicles; net worth is assets minus debts, counted once each |
| timeline | no throw, no NaN or Infinity, on any valid household (3 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| unlearning | no throw, no NaN or Infinity, on any valid household (3 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| values | no throw, no NaN or Infinity, on any valid household (1 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| variableincome | no throw, no NaN or Infinity, on any valid household (4 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| variance | no throw, no NaN or Infinity, on any valid household (3 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| vpw | the withdrawal percentage sits in (0, 1] and never falls with age; a plan never throws, never returns NaN, and is the same twice |
| week | no throw, no NaN or Infinity, on any valid household (8 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |
| windfall | investing at once beats spreading it out whenever the market rate is at least the cash rate; the same input gives the same run twice, and every path cell is whole cents |
| worth | no throw, no NaN or Infinity, on any valid household (4 functions); the same input gives the same output twice (no hidden state); the household passed in is byte-identical afterwards; cents in, cents out: no *Cents field carries a fraction |

## Section 3: sourced data tables

3957 checks over 8 files. Source: `tests/data.test.js`. A note is a disagreement between a lane 2 table and the copy an engine reads today, or a cell the rule had to make an exception for; each carries a DECIDE:.

### Failures

None.

### Notes

- states.json: 408 cells
- lane2/milestones.json: 10 cells
- lane2/aca.json: 15 cells
- lane2/studentloans.json: 40 cells
- lane2/contribution_limits.json: 46 cells
- lane2/tax_brackets.json: 38 cells
- return_bands.json: 1 cells
- bands.json: 3 cells
- 45 prior-year cells (2025 rows) are older than 18 months and marked `historical: true`: a closed year is a settled fact, not a stale one. DECIDE: whether the 18-month rule should read that way.
- 51 cells are older than 18 months and say so (`stale: true` with a DECIDE: note); every one is the childcare column, whose 2024 edition this session could not open. Not a pass: a flag for the May refresh.
- states.json says OH is flat for 2026; state_brackets_2026.json says brackets (2025 edition): the 2026 change is not yet in the engine table
- UI benefit cells differ from data/ui_benefits.json (the engine copy) in 27 states; states.json carries the July 2025 DOL edition plus the October 2025 increases, ui_benefits.json a 2025 recollection. DECIDE: which the engine reads.
- 2026 top applicable percentage: aca.json says 0.0996 (Rev. Proc. 2025-25), aca_2026.json says 0.0866. DECIDE: which the engine reads.
- 2026 annualAdditions415c: contribution_limits.json says 72000 (IRS Notice 2025-67 as read from search), irs_limits_2026.json says 70000 (carried from the FOO room). DECIDE: which the engine reads.

## Section 4: the gloss dictionary and the lookup sentences

258 glossary entries, 3069 checks. Source: `tests/glossary.test.js`.

### Failures

None.

### Notes

- "cliff" is listed under both "cliff vesting" and "subsidy cliff"; get() returns the first
- "FAT" is listed under both "Fat FIRE" and "FAT"; get() returns the first
- "percentile" is listed under both "net worth percentile" and "percentile"; get() returns the first
- glossary reading level: Flesch-Kincaid grade 7.2 (16.7 words a sentence, 1.38 syllables a word) over 258 definitions

## Section 5: the migration corpus

70 export files across 40 formats, 905 checks. Source: `tests/migration.test.js`; corpus from `tests/tools/build-exports.js` (index in `fixtures/exports/README.md`).

### Files that refuse to import or disagree with a known value

| file | field | expected | got | why |
|---|---|---|---|---|
| 2026-09-02-pre-spine-flat-profile.json | importJSON | a way in | refused: That file has no household in it. | the pre-spine flat profile (annualSalary, studentLoanBalance) only migrates from localStorage via _migrateLegacy; a person who saved one as a file cannot import it. DECIDE: whether Your Data should accept it. |

### Throws and unreadable fixtures

None.

### Notes

- 1 of 70 fixture files disagree with a known value or refuse to import; each row above names the field.

