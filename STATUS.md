# STATUS (keep under 40 lines; /wrap rewrites it)

Updated: 2026-09-19

## Where it stands
- **93 → 30 is the programme.** The reframe (D-228): you cannot size the
  mountain until you know how you come down it, so the Back Half defines
  the target. The thirty are D-229 and `docs/room-map.json`, checked against
  the registry every run: `absorbs` minus `done` is what is left.
- **Step 1 is finished and 76 registry rooms are left.** D-234 answered the
  question that sat at the top of this file since the merge began: who owns
  Start Here's seventeen fields. The app's own data answered it, not taste.
  Every row in `data/ledger-rows.json` declares its DAITE family and six
  name a room in `askIn`. Two were computed (`age`, `capturingFullMatch`),
  so fifteen moved, each with the box that asks it: Tax, Settings, Debt,
  the Cushion, Income, Where It Goes, the Statement. Start Here writes
  nothing and is a redirect carrying each old question to its new page.
- Ledger rework brief: built through Phase K; H6, I6, I7, I8 wait. On the
  owner: the Pages switch, the domain (G1.1), a LICENSE (G3.14).
- The book's companion suite: a brief and four owner decisions in
  `docs/sparks-tools-brief.md`. Nothing built until the merge is finished.

## Freeze
- ON, and the merge is how it is honoured: every session leaves fewer screens.

## Next (top item first; one per session)
1. **Step 5: the Decision Room shell.** One shell, five outputs on every
   block, then goals, wedding and big purchase before any deep module.
2. Step 6: the Back Half, once the shell stops moving. D-228 is its brief.
3. `Gate.CARDS` is orphaned: only the one-pager built from it, while
   `Gate.fillGuesses` is still used everywhere. A `/drift` pass.
4. Older and still open: logged income does not reach the headline numbers
   (gross and log are in one room now); the first journey end to end; a
   monthly close that prompts a backup.

## Panel notes not yet answered
- Galloway: model the means-tested floor, or the Back Half sells comfort.
- Donegan: the merged Work room holds eight rooms about having a job and
  none about building something that pays you.
- Hormozi: thirty is still a library. One screen in front of it.
