# STATUS (keep under 40 lines; /wrap rewrites it)

Updated: 2026-09-13

## Where it stands
- **93 → 38 rooms.** `docs/room-map.json` is the cut list, checked against
  the registry every run: **55 absorptions shipped, 8 held with the reason
  written down, none unaccounted for.**
- All six sequenced steps shipped. Step 5 built the Decision Room shell —
  five questions of every block (D-253); step 6 merged The Back Half and
  The Number.
- **The reframe D-228 asks for has NOT shipped**, and both rooms say so in
  their spec: the FI target is still spending ÷ the withdrawal rate.
- The phone walk finds what the static suite cannot: four bugs in D-252,
  three more in D-256/257/258. Run it after every merge — a merge changes
  what a room needs and who it is for, and neither shows in the diff.
- D-259: D-238…D-258 were written `###`, so the index and `pack.js` could
  not see them. Fixed; the build fails on a deeper heading now.
- **Freeze ON, and honoured**: every step left fewer screens.

## Next (top item first; one per session)
1. **OWNER DECISION: is the income floor means-tested?** (D-228, D-254.)
   The FI target waits on it, and it moves the number every room shows.
   Nothing else is blocked on anything.
2. **OWNER DECISION: who owns Start Here's 17 fields** when it retires
   into the Ledger. The last room of step 1.
3. Wedding as the next block type: a block carrying a per-unit figure.
4. Where rollover belongs — held out of Work ("not retired"), which it
   never was. D-254 names The Statement.

## Known open
- Nothing. `node test/forms.js`: 619 checks, none failing — the two logged
  here as pre-existing were D-257's bug at the nearest hundred.

## Panel notes not yet answered
- Galloway: the means-tested floor, above. It is now the top item.
- Donegan: Work is seven readings about having a job, none about building
  something that pays you. The strip makes that plainer.
- Hormozi: thirty-eight is still a library. One screen in front of it.
