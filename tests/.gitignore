# The root .gitignore hides every package.json; this one is the lane 2
# test harness (fast-check for the property tests, section 2) and belongs
# in the repo. node_modules stays ignored.
!package.json
!package-lock.json
node_modules/
