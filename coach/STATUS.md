# Coach Mode status

Updated: 2026-09-26

## Where it stands
- Its own app (CD-001): three screens, its own store (`coach.` keys), tests
  (`node coach/test/run.js`, in CI) and log. SPARKS is untouched; the only
  SPARKS files that mention it are `CLAUDE.md`, `STATUS.md`, D-339, the CI
  step and the `.gitignore` rules.
- The audit and UX pass (CD-009 to CD-011, `AUDIT.md`): plain words with a
  "?" on every box, read-out and stop; a chart for every number set, each
  with Customise (type, theme, a swatch a series), a table view and tips;
  a header, a welcome, client cards, a step bar, one note box; a print view.
- Built before that: roster, demo client, sheet import, sealed backups; the
  Session with its entry forms, rail, detour, quick entry, notes, homework,
  comments, decisions and the recap; Client View with the life map and the
  check-in.

## Next
1. The owner tries it with the demo client, then with one real client, and
   says which words still land wrong.
2. Owner questions: is "match 4%" right as dollar for dollar up to 4% of pay?
   Check-in windows (in by 35 days, late by 65) right for a monthly rhythm?
3. Later, not now: the client portal (`LATER.md`).
